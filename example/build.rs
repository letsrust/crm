use anyhow::Result;

fn main() -> Result<()> {
    // prost_build::compile_protos(&["../protos/example/items.proto"], &["../protos/"])?;
    // let mut build_config = prost_build::Config::new();
    // build_config
    //     .out_dir("src/pb")
    //     .compile_protos(&["../protos/example/items.proto"], &["../protos/"])?;

    Ok(())
}
